package bl.helper;

import PO.StockData;
import VO.StockDataVO;

public class StockPoToVo{
	
     public static StockDataVO stockDataToStockDataVO(StockData stockData){
//    	 StockDataVO stockDataVO=new StockDataVO(stockData.get, stockData.getId(), , highValue, lowValue, openValue, closeValue, turnoverRate, marketRate, priceEarningsRatio)
		return null;
    	 
     }
}
